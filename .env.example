# List of environment variables this project needs (key names only, committed without values)
# Store the actual values in sealbox: printf '%s' "value" | sealbox set project/KEY_NAME
# Run with: sealbox run -p project -- CMD
# API_KEY=
# DATABASE_URL=
